<resources>
    <!-- Main App Theme (Material3, Night) -->
    <style name="AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/default_neutral_lighter</item>
        <item name="elevationOverlayEnabled">false</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">@color/status_bar_background</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay.Material3</item>
        <item name="preferenceFragmentCompatStyle">@style/AppPreferenceFragmentCompatStyle</item>
    </style>

    <!-- Preference Theme Overlay for Material3 (Night) -->
    <style name="PreferenceThemeOverlay.Material3" parent="PreferenceThemeOverlay.v14.Material">
        <item name="colorAccent">@color/default_neutral_lighter</item>
        <item name="android:textColor">@color/md_theme_onSurface</item>
        <item name="android:background">@color/md_theme_surface</item>
    </style>

    <!-- Style override for PreferenceFragmentCompat (Night) -->
    <style name="AppPreferenceFragmentCompatStyle" parent="PreferenceThemeOverlay.Material3" />
</resources>
